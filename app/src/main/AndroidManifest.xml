<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.getcounrtyappretrofitexample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/storeappicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/storeappicon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Sopping_cart_Activity"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".Item_Grid_Activity" />
        <activity android:name=".Item_List_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>